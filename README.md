# repair **Desktop Environment**
---
`sudo apt-get update` for update software in system.

`sudo apt-get upgrade -y` for upgrade ypur system.

`sudo apt-get install -f gdm3` for download Desktop Environment

